language: python
python:
  - "3.6"
# command to run tests
install:
  - pip install selenium
  - pip install --user phantomjs
  #- python -m pip install phantomjs
  - pip install
  
script:
  - python3 kentjobsearch.py
